# Recipes API

**Status**: NO ENDPOINTS EXIST
**Investigated**: INV-018 (Session 5)
**Note**: `canReadRecipe` and `canManageRecipe` abilities exist, but no REST endpoints.

## Paths Tested (All 404)
- `GET /v3/recipes`
- `GET /v3/recipes?workspaceId=`
- `GET /v3/workspaces/{id}/recipes`
