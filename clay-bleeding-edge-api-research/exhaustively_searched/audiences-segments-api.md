# Audiences / Segments API

**Status**: NO ENDPOINTS EXIST
**Investigated**: INV-018 (Session 5)
**Note**: `audienceAbilities` exist on workspace, but the feature is disabled (`enableAudiences: false`).

## Paths Tested (All 404)
- `GET /v3/audiences`
- `GET /v3/audiences?workspaceId=`
- `GET /v3/workspaces/{id}/audiences`
- `GET /v3/segments`
- `GET /v3/workspaces/{id}/segments`
