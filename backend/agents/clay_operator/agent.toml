slug = "clay_operator"
name = "Clay Operator"
description = "Designs, builds, and manages Clay workbooks end-to-end: creates tables, columns, formulas, enrichment configs, webhook sources, and action columns via the v3 API. Full CRUD on tables, rows, columns, sources, and enrichments. Fully automated when session cookie is configured."
intents = ["clay", "clay table", "clay setup", "clay enrichment", "clay webhook", "clay social listening", "configure clay", "create clay table", "clay action column", "clay formula"]
max_iterations = 100
skip_judge = false
automation_mode = "full"
required_integrations = ["clay"]
