slug = "data_pipeline_builder"
name = "Data Pipeline Builder"
category = "program_operations"
description = "Builds, audits, and repairs data pipelines connecting multiple systems (Clay, Supabase, n8n, CRM). Audits cross-system data quality, diagnoses broken flows, maps source tables, implements webhooks and conditional routing, and handles edge cases like deduplication and schema changes."
intents = ["data pipeline", "connect systems", "data flow", "webhook setup", "data sync", "integration pipeline", "etl", "data routing", "sync data between", "connect clay to", "data audit", "cross-check data", "missing records", "data consistency", "data quality", "verify data", "reconcile data", "diagnose pipeline", "data not flowing", "broken pipeline", "debug data flow", "fix data flow", "pipeline error", "stale data"]
max_iterations = 20
skip_judge = false
flexible_tool_use = true
expert_slug = "lele"
required_integrations = ["n8n", "tavily"]
