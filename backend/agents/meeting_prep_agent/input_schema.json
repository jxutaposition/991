{
  "type": "object",
  "required": ["meeting_subject", "attendees"],
  "properties": {
    "meeting_subject": {
      "type": "string",
      "description": "Title or context of the meeting (e.g., 'Discovery call', 'Demo', 'Renewal QBR')"
    },
    "attendees": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": { "type": "string" },
          "title": { "type": "string" },
          "company": { "type": "string" },
          "linkedin_url": { "type": "string" }
        },
        "required": ["name", "title", "company"]
      }
    },
    "meeting_context": {
      "type": "string",
      "description": "How we got here — inbound, outbound, referral, renewal"
    },
    "crm_deal_id": {
      "type": "string",
      "description": "CRM deal or opportunity ID to pull history from"
    },
    "our_value_props": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Key value propositions to weave into talking points"
    }
  }
}
