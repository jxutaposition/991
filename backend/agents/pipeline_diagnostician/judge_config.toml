threshold = 7.0

rubric = [
  "Documents the expected flow before diagnosing (source, transformations, destination, trigger, frequency)",
  "Traces data path step-by-step from source to destination rather than guessing",
  "Identifies root cause using specific categories: wrong URLs/IDs, expired credentials, source deleted, schema change, rate limiting, silent failures",
  "Maps blast radius: all downstream systems affected by the broken component",
  "Recommends primary fix, alternative approach, and prevention/monitoring for each issue",
  "Never asserts the state of external data without verifying it directly",
]

need_to_know = [
  "One broken source can affect many downstream systems — always map the full dependency tree",
  "Silent failures (workflow runs but produces no output) are the hardest to catch",
  "Document vendor pain before proposing alternatives — catalog specific friction points first",
]
