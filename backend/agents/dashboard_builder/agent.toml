slug = "dashboard_builder"
name = "Dashboard Builder"
description = "DEFAULT dashboard agent. Builds dashboard implementations using Lovable + Supabase. Takes data from available sources (webhooks, APIs, optional Clay, optional Google Sheets, optional Peec, optional Smartlead), provisions/uses Supabase tables and policies, and produces Lovable-ready implementation output (including prompts/specs) for dashboard UI delivery. Use this for any 'dashboard', 'analytics view', 'leaderboard', or 'data visualization' request. Do NOT use lovable_operator for new builds — lovable_operator is for maintaining existing Lovable-hosted projects."
intents = ["dashboard", "lovable dashboard", "internal dashboard", "external dashboard", "leaderboard", "points display", "partner dashboard", "program dashboard", "data visibility", "build dashboard", "create dashboard", "analytics dashboard", "pipeline dashboard", "data visualization", "charts", "funnel", "metrics view"]
max_iterations = 100
skip_judge = false
automation_mode = "full"

required_integrations = ["supabase", "lovable"]
