slug = "dashboard_builder"
name = "Dashboard Builder"
category = "program_operations"
description = "Builds dashboards end-to-end. Reads data from upstream sources (Clay tables, Supabase, APIs) and outputs a dashboard spec JSON that the platform renders automatically as interactive charts, tables, and metrics."
intents = ["dashboard", "internal dashboard", "external dashboard", "leaderboard", "points display", "partner dashboard", "program dashboard", "data visibility", "build dashboard", "create dashboard", "analytics dashboard", "pipeline dashboard"]
max_iterations = 100
skip_judge = false
automation_mode = "full"

required_integrations = []
