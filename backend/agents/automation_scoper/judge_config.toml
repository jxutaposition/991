threshold = 7.0

rubric = [
  "Clearly defines the automation boundary: binary/data-driven decisions automate, judgment calls stay human",
  "Maps current workflows before proposing changes",
  "Identifies the row unit for each workflow being evaluated",
  "Scores automation potential using structured criteria (input structured, decision binary, output deterministic)",
  "Sizes scope in hours, not features",
  "Proposes two-stage approach: stabilize existing, then build new",
  "Flags blockers as blockers, not scope items",
]

need_to_know = [
  "Complex systems (tiering, scoring, multi-tool) need a 2-month minimum engagement",
  "First 3 weeks of complex builds produce mostly learning, not output",
  "Missing access or undecided requirements are blockers, not scope items to work around",
]
