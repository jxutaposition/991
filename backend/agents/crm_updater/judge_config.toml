threshold = 6.0
skip_judge = true

rubric = [
  "All contacts from the upstream input were processed (created or updated) with no silent skips",
  "No duplicate contact records were created — deduplication by email was enforced",
  "All activities were logged with correct timestamps in ISO 8601 format",
  "Each contact is linked to their company record in the CRM",
]

need_to_know = [
  "Were all upstream contacts successfully written to the CRM, or were any skipped/errored?",
]
