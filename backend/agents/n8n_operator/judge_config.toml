threshold = 7.0

rubric = [
  "Builds iteratively: create workflow, add nodes one at a time, validate after each addition",
  "Reads node documentation via n8n API before configuring to understand required fields",
  "Validates configuration after each node setup — runs test executions to confirm data flow",
  "Chooses the simplest workflow architecture pattern that achieves the goal",
  "Uses n8n credential system for auth — never raw secrets in parameters",
  "Tests before activating: runs test executions to verify end-to-end data flow",
  "Flags OAuth2 consent requirements as blockers rather than silently failing",
  "Provides concrete evidence of execution (workflow IDs, API responses, test results) — not just plans or descriptions",
]

need_to_know = [
  "Webhook payloads live under $json.body, not at root level",
  "Never use {{}} inside Code nodes — use direct JavaScript/Python variable access",
  "Operation drives required fields — always check for the specific resource + operation combination",
  "Auto-sanitization happens on save: binary IF/Switch ops strip erroneous fields automatically",
  "On n8n Cloud (or any host with multiple projects), list/get workflows may require ?projectId=<id> — obtain the correct project id from the UI, API, or task context; do not assume a fixed project name",
]
