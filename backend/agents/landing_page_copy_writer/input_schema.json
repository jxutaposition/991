{
  "type": "object",
  "required": ["product_name", "product_description", "target_audience"],
  "properties": {
    "product_name": { "type": "string" },
    "product_description": { "type": "string" },
    "target_audience": { "type": "string" },
    "key_message": { "type": "string" },
    "creative_brief": { "type": "string" },
    "ad_copy_driving_traffic": { "type": "string", "description": "The ad headline/copy that will send traffic here — for message match" },
    "customer_outcomes": { "type": "array", "items": { "type": "string" } },
    "top_features": { "type": "array", "items": { "type": "string" } },
    "primary_cta": { "type": "string", "description": "What action the page should drive — 'book a demo', 'start free trial', etc." },
    "top_objections": { "type": "array", "items": { "type": "string" } }
  }
}
