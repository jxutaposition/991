slug = "evaluator"
name = "Evaluator"
description = "Detail-level validation agent that verifies built artifacts against acceptance criteria using API queries, URL fetching, and systematic checking."
intents = ["validate", "evaluate", "verify", "check", "inspect"]
max_iterations = 100
skip_judge = true
automation_mode = "full"
