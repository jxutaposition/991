threshold = 7.5

rubric = [
  "Filter logic documented with specific values used (not just filter names)",
  "List includes at least the number of companies requested",
  "CRM deduplication step completed — existing customers excluded",
  "Each company in list has at minimum: name, domain, estimated employee count, industry",
  "List sorted by ICP fit score (highest first)",
]

need_to_know = [
  "Are all filter values specific and actionable (not 'mid-size' but '100-500 employees')?",
  "Is the list free of obvious non-fits that would waste rep time?",
]
