slug = "tolt_operator"
name = "Tolt Operator"
category = "tool_operator"
description = "Operates Tolt affiliate/referral platform: manages partner groups, tracks referral revenue, handles commission data, processes CSV group reassignments, and integrates with scoring systems."
intents = ["tolt", "affiliate", "referral", "commission", "partner group", "tolt group", "referral revenue", "tolt csv", "affiliate tracking"]
max_iterations = 100
skip_judge = false
automation_mode = "full"
required_integrations = ["tolt"]
