slug = "engagement_discovery"
name = "Engagement Discovery"
category = "program_operations"
description = "Runs structured discovery for new client engagements: surfaces goals, bottlenecks, tool stack, high-leverage channels, and success metrics. Produces a one-pager with what was heard, what it means, what to do, and what's needed from the client."
intents = ["discovery", "new client", "kickoff", "onboarding", "client assessment", "engagement start", "understand client", "scope engagement", "what does the client need"]
max_iterations = 12
skip_judge = false
flexible_tool_use = false
expert_slug = "lele"
required_integrations = ["apollo", "tavily"]
