id = "lovable"
name = "Lovable"
category = "frontend-hosting"
description = "Cloud-hosted web app builder with AI chat editor. Projects are Vite + React + TypeScript + Supabase. No REST API for project editing — changes are made via GitHub sync (push to connected branch) or by generating chat prompts for the user to paste into the Lovable editor."
required_credentials = ["supabase"]
enabled = true

[tradeoffs]
automation = "partial"
api_access = "none"
cost = "free-tier-available"
notes = "No project creation API. No editing API. Changes deploy immediately (no staging). GitHub sync enables programmatic code changes if the project is connected to a repo. Manual project creation required via lovable.dev UI."
